(library
 ((name ppx_driver)
  (public_name ppx_driver)
  (public_release ((libraries (:standard \ resrap))))
  (wrapped false)
  (libraries (ocamlcommon ppx_core ppx_optcomp resrap))
  (preprocess ((no_preprocessing All)))))
